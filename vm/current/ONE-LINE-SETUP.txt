# 🚀 COPY-PASTE INTO VM TERMINAL (ONE LINE AT A TIME)

# 1. Quick UTM + Network Setup
sudo dhcp.sh && tce-load -wi git curl wget bash

# 2. Clone and Install uDOS  
git clone https://github.com/fredporter/uDESK.git && cd uDESK && ./vm/current/install.sh

# 3. Add Auto-Startup (optional)
echo 'export PATH="/usr/local/bin:$PATH"' >> ~/.profile && echo 'udos init >/dev/null 2>&1 && echo "🚀 uDOS Ready!"' >> ~/.profile

# 4. Setup UTM Shared Folder (if using UTM)
sudo mkdir -p /mnt/shared && sudo mount -t 9p -o trans=virtio,version=9p2000.L share /mnt/shared

# 5. Test Everything Works
udos version && udos role && echo "✅ M1 Complete - Ready for M2!"

# SHORTCUTS AFTER SETUP:
# u = udos, uh = udos help, ui = udos info, ur = udos role
