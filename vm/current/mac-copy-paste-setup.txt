# uDOS VM Quick Setup Commands
# Copy-paste these commands in your TinyCore VM

# 1. Add auto-startup to profile (one-time setup)
echo 'export PATH="/usr/local/bin:$PATH"' >> ~/.profile
echo 'udos init >/dev/null 2>&1; udos role detect >/dev/null 2>&1' >> ~/.profile
echo 'echo "🚀 uDOS Ready! Type: udos help"' >> ~/.profile

# 2. Quick commands for immediate use
alias u='udos'
alias uv='uvar'
alias ud='udata'
alias uh='udos help'
alias ui='udos info'
alias ur='udos role'

# 3. Test everything works
udos version && echo "✅ M1 Complete!"
