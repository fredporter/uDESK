# 🚀 BOOTSTRAP COMMANDS FOR FRESH TINYCORE VM
# Copy-paste these commands FIRST (before git-deploy.sh)

## Step 1: Enable Networking
sudo dhcp.sh

## Step 2: Install Essential Tools (Required for Git deployment)
tce-load -wi curl.tcz
tce-load -wi bash.tcz  
tce-load -wi git.tcz
tce-load -wi wget.tcz

## Step 3A: Run POSIX-Compatible Deployment (RECOMMENDED)
curl -sL https://raw.githubusercontent.com/fredporter/uDESK/main/vm/current/git-deploy-posix.sh | sh

## Step 3B: Alternative - Download and Run Manually
wget https://raw.githubusercontent.com/fredporter/uDESK/main/vm/current/git-deploy-posix.sh
chmod +x git-deploy-posix.sh
./git-deploy-posix.sh

## Step 3C: Original Script (if bash is available)
curl -sL https://raw.githubusercontent.com/fredporter/uDESK/main/vm/current/git-deploy.sh | bash

# ═══════════════════════════════════════════════════════════════
# ALTERNATIVE: Manual Download Method (if curl fails initially)
# ═══════════════════════════════════════════════════════════════

## If curl isn't available yet, use wget:
wget https://raw.githubusercontent.com/fredporter/uDESK/main/vm/current/git-deploy.sh
chmod +x git-deploy.sh
./git-deploy.sh

# ═══════════════════════════════════════════════════════════════
# COMPLETE ONE-BY-ONE SEQUENCE (Most Reliable)
# ═══════════════════════════════════════════════════════════════

# 1. Network
sudo dhcp.sh

# 2. Core tools
tce-load -wi curl.tcz

# 3. Additional tools  
tce-load -wi bash.tcz git.tcz wget.tcz nano.tcz

# 4. Deploy uDOS
curl -sL https://raw.githubusercontent.com/fredporter/uDESK/main/vm/current/git-deploy.sh | bash

# 5. Test
udos version && echo "✅ Success!"
